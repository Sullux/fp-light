const {
  Specification: S,
} = require('./specification')

describe('core', () => {
  describe('specification', () => {
    describe('Specification', () => {
      describe('Function', () => {
        describe('isImplemented', () => {
          it('todo', () => {})
        })
        describe('isCompatible', () => {
          it('todo', () => {})
        })
        describe('delta', () => {
          it('todo', () => {})
        })
      })
    })
  })
})
