// const { Scope } = require('./scope')
// const { Spreadable } = require('./spreadable')
// const { Identity } = require('./identity')

// function Fn (...parameters) {
//   if (parameters.length === 0) { return undefined }
//   const last = parameters.length - 1
//   const assertions = parameters.slice(0, last)
//   const fn = parameters[last]
//   if (impl === undefined) { return undefined }
//   if (impl.length === 0) { return undefined }
//   if (impl.length === 1) { return impl[1] }
// }

// call the function directly and error if missing args
const call = () => {}

// apply args to the function and get back a result or a new function
const apply = () => {}
