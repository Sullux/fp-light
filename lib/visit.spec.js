const {
  isVisitable,
  visit,
} = require('./visit')

describe('visit', () => {
  it('should visit all nested values in objects and arrays', () => {
    const visits = []
    const callback = info => visits.push(info)
  })
})
