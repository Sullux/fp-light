{
  "name": "@sullux/fp-light-reduce",
  "version": "0.0.1",
  "engines": { "node": ">=8.*" },
  "description": "A lightweight functional library for functional composition and object composition.",
  "keywords": ["fp", "functional", "reduce"],
  "main": "index.min.js",
  "unpkg": "index.min.js",
  "scripts": {
    "build": "node-minify --compressor terser --input 'reduce.js' --output 'dist/index.min.js'",
    "package": "npm run build && node ../../package.js && cp README.md dist/",
    "unit": "../../node_modules/.bin/mocha *.spec.js",
    "lint": "../../node_modules/.bin/eslint *.js",
    "test": "npm run lint && npm run unit",
    "deploy": "npm test && npm run package && cd dist && npm publish --access public"
  },
  "author": "Charles Sullivan <sullux@gmail.com> (https://github.com/Sullux)",
  "license": "MIT",
  "repository": "sullux/fp-light",
  "homepage": "https://github.com/Sullux/fp-light/blob/master/lib/reduce/README.md"
}
