{
  "name": "@sullux/fp-light",
  "version": "1.2.33",
  "engines": {
    "node": ">=10.*"
  },
  "description": "A lightweight library to support functional programming in javascript.",
  "keywords": [
    "fp",
    "functional"
  ],
  "main": "index.js",
  "scripts": {
    "build": "(rm -rf dist || 0) && mkdir dist && rollup -c && cp README.md dist/ && node ./scripts/package.js",
    "lint": "yarn standardx",
    "fix": "yarn standardx --fix",
    "test": "jest --verbose --passWithNoTests",
    "deploy": "yarn build && npm publish dist --access public"
  },
  "author": "Charles Sullivan <sullux@gmail.com> (https://github.com/Sullux)",
  "license": "MIT",
  "repository": "sullux/fp-light",
  "homepage": "https://github.com/Sullux/fp-light/blob/master/README.md",
  "devDependencies": {
    "jest": "^28.1.1",
    "rollup": "^2.53.0",
    "standardx": "^7.0.0"
  },
  "eslintConfig": {
    "rules": {
      "comma-dangle": [
        "error",
        "always-multiline"
      ]
    },
    "parserOptions": {
      "sourceType": "module",
      "ecmaVersion": 2021
    }
  },
  "standardx": {
    "env": [
      "jest",
      "node",
      "es2021"
    ]
  },
  "dependencies": {
    "wabt": "^1.0.32"
  }
}
